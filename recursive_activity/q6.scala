import scala.io.StdIn.{readLine, readInt}
import scala.math._
import scala.Array._
import util.control.Breaks._
import scala.collection.mutable._

object q6{
    def feb(num:Int = 10, pre:Int = 0): Unit = {
        if ( num ==  )
    }

    def main(args:Array[String]): Unit = {// main

    }
}